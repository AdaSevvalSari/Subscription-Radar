{
  "name": "subscription-radar-backend",
  "version": "1.0.0",
  "description": "Subscription Radar - Personal Subscription Tracking System",
  "main": "src/app.js",
  "scripts": {
    "start": "node --no-warnings src/app.js",
    "dev": "nodemon --no-warnings src/app.js",
    "test": "jest --verbose",
    "lint": "eslint src/ tests/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
